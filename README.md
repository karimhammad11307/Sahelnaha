سهلناها - نظام الإشراف التربوي
نظام شامل لإدارة الإشراف التربوي في سلطنة عمان، يهدف لتسهيل عمل المشرفين والمعلمين.

🚀 المميزات الرئيسية
إدارة الزيارات الإشرافية والتقييمات
نظام الرسائل والتواصل
إدارة المعلمين والمدارس
تقارير شهرية واحصائيات
مساعد ذكي لإعداد الدروس (AI)
منصة الأخبار والفعاليات
بطاقات التهنئة والشارات
نماذج تفاعلية قابلة للتخصيص
📋 المتطلبات
Node.js (الإصدار 16 أو أحدث)
npm أو yarn
حساب Supabase (للقاعدة البيانات)
حساب OpenAI (للمساعد الذكي - اختياري)
🔧 التثبيت
استنساخ المشروع:
git clone [repository_url]
cd sahalnaha
تثبيت المكتبات:
npm install
إعداد متغيرات البيئة:
انسخ ملف .env.example إلى .env
املأ القيم المطلوبة:
cp .env.example .env
تشغيل المشروع:
npm run dev
🗄️ قاعدة البيانات (Supabase)
المشروع يستخدم Supabase كقاعدة بيانات. جميع الـ migrations موجودة في:

supabase/migrations/
إعداد قاعدة البيانات:
أنشئ مشروع جديد على Supabase
انسخ URL و ANON KEY من Project Settings → API
ضعهم في ملف .env
قم بتطبيق الـ migrations من لوحة Supabase SQL Editor
🏗️ بنية المشروع
src/
├── components/     # المكونات المشتركة
├── contexts/       # React Contexts (Auth, Cache)
├── data/           # البيانات الثابتة
├── lib/            # المكتبات المساعدة (Supabase, PDF, الخ)
├── pages/          # صفحات التطبيق
├── utils/          # دوال مساعدة
└── main.tsx        # نقطة الدخول

supabase/
├── functions/      # Edge Functions
└── migrations/     # قاعدة البيانات

public/
├── fonts/          # الخطوط العربية
└── images/         # الصور والشعارات
🔑 المفاتيح المطلوبة
في ملف .env:

VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key
VITE_OPENAI_API_KEY=your_openai_key (اختياري)
📦 البناء للإنتاج
npm run build
الملفات المبنية ستكون في مجلد dist/

🛠️ التقنيات المستخدمة
React + TypeScript - الواجهة الأمامية
Vite - أداة البناء
Tailwind CSS - التصميم
Supabase - قاعدة البيانات والمصادقة
React Router - التوجيه
html2pdf.js - تصدير PDF
OpenAI - المساعد الذكي
Recharts - الرسوم البيانية
👥 المساهمة
للمساهمة في المشروع:

أنشئ branch جديد للميزة
قم بالتعديلات المطلوبة
اختبر التعديلات
أنشئ Pull Request
📝 ملاحظات مهمة
المشروع مصمم خصيصاً للمديريات التعليمية في سلطنة عمان
يدعم اللغة العربية بالكامل (RTL)
يتطلب صلاحيات مشرف للوصول لمعظم الميزات
نظام RLS مفعّل في Supabase لحماية البيانات
